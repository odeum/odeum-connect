sudo: false
language: node_js
node_js:
    - stable
cache:
    directories:
        - node_modules
notifications:
    email: true
branches:
    only:
        - master
script:
  - npm run build
  - npm run push